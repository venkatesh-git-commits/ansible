- hosts: DEV
  connection: ssh
  tasks:
    - name: create a user
      user: name={{item}} state=present

      with_items:
        - Jenkins
        - docker
        - Kubernetes
        - terraform
        - azure
