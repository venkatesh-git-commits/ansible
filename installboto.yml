
- hosts: dev[0]
  connection: ssh
  tasks:
    - name: install pip module
      yum: name=python3-pip state=present ### letting install python-pip on slave server or else it will throw us an error that it doesn't exist on slave

    - name: install boto module on dev group
      pip: name=boto3


